import React from "react"
import styles from "../styles/login.module.css"

const LoginPage = () => {
	return (
		<div className={styles.container}>
			<h2 className={styles.heading}>Formulário de Login</h2>
		</div>
	)
}

export default LoginPage
